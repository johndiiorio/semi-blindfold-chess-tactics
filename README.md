# Semi-Blindfold Chess

A web application experiment.

## Development Quick-start

1. Install yarn and node.
2. Create a `.env` file in the root directory that looks like:
```
SBCT_TOKEN_SECRET=your_secret_token
```
3. Execute:
```
yarn
yarn build
yarn start
```